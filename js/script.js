// criando uma contagem com for
for (let i = 0; i <= 9; i += 2) {
	console.log(i);
}

// Sorteando com For
for (let i = 0; i <= 19; i++) {
	console.log(`Número Sorteado: ${Math.floor(Math.random() * 20)}`);
}
